---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: racknas-media
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  volumeName: racknas-media
  resources:
    requests:
      storage: 50Ti
