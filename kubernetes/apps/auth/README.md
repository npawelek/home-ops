# Authentik Authentication Platform

This directory contains the Authentik deployment with CloudNative-PG PostgreSQL and built-in Redis.

## Access

After deployment, access Authentik at `https://auth.${SECRET_DOMAIN}/if/flow/initial-setup/` (note trailing slash)
