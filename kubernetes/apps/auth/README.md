# Authentik Authentication Platform

This directory contains the Authentik deployment with CloudNative-PG PostgreSQL.

## Access

For initial setup, access Authentik at `https://auth.${SECRET_DOMAIN}/if/flow/initial-setup/` (note trailing slash)
