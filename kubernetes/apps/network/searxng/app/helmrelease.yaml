# yamllint disable-line rule:line-length
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/app-template-5.2.1/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: searxng
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 1h
  values:
    global:
      createDefaultServiceAccount: false
    controllers:
      searxng:
        annotations:
          reloader.stakater.com/auto: "true"
        type: deployment
        replicas: 2
        strategy: RollingUpdate
        initContainers:
          config-init:
            image:
              repository: public.ecr.aws/docker/library/alpine
              tag: 3.24.2@sha256:294b683cb724975bec92580e1e685676bd4b50bda910ddb8c51d4cabeaec77e6
            command:
              - /bin/sh
              - -c
              - |
                sed \
                  -e "s|\$${BRAVE_API_KEY}|$${BRAVE_API_KEY}|g" \
                  -e "s|\$${KAGI_API_KEY}|$${KAGI_API_KEY}|g" \
                  /tmp/settings-template/settings.yml \
                  > /etc/searxng/settings.yml
            envFrom:
              - secretRef:
                  name: searxng-secret
        containers:
          app:
            image:
              repository: ghcr.io/searxng/searxng
              tag: 2026.9.18-c0042add3@sha256:e0027a772aeeea55bf642256aae6fb3344ffa5f25ca665898c2ea821101334c4
            env:
              TZ: ${TIMEZONE}
              http_proxy: ${HTTP_PROXY}
              https_proxy: ${HTTPS_PROXY}
              no_proxy: ${NO_PROXY}
              SEARXNG_BASE_URL: "https://{{.Release.Name}}.${SECRET_DOMAIN}"
              SEARXNG_PORT: &port 8080
            envFrom:
              - secretRef:
                  name: searxng-secret
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: *port
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - "ALL"
            resources:
              requests:
                cpu: 10m
                memory: 256Mi
              limits:
                memory: 2048Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 977
        runAsGroup: 977
        fsGroup: 977
        fsGroupChangePolicy: OnRootMismatch
    persistence:
      config:
        type: configMap
        name: "{{.Release.Name}}-configmap"
        advancedMounts:
          searxng:
            config-init:
              - path: /tmp/settings-template/settings.yml
                subPath: settings.yml
                readOnly: true
            app:
              - path: /etc/searxng/limiter.toml
                subPath: limiter.toml
                readOnly: true
      config-processed:
        type: emptyDir
        advancedMounts:
          searxng:
            config-init:
              - path: /etc/searxng
            app:
              - path: /etc/searxng
      ca-bundle:
        type: configMap
        name: internal-ca-bundle
        globalMounts:
          - path: /etc/ssl/certs/ca.crt
            subPath: ca.crt
            readOnly: true
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
    service:
      app:
        ports:
          http:
            port: *port
    route:
      app:
        hostnames: ["{{.Release.Name}}.${SECRET_DOMAIN}"]
        parentRefs:
          - name: envoy-internal
            namespace: gateway
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
