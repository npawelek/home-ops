---
apiVersion: v1
kind: Service
metadata:
  name: hdhomerun
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: hdhomerun
  labels:
    kubernetes.io/service-name: hdhomerun
addressType: IPv4
endpoints:
  - addresses:
      - ${HDHOMERUN_ADDR}
ports:
  - name: http
    port: 80
    protocol: TCP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hdhomerun
spec:
  hostnames:
    - hdhomerun.${SECRET_DOMAIN}
  parentRefs:
    - name: envoy-internal
      namespace: gateway
      sectionName: https
  rules:
    - backendRefs:
        - name: hdhomerun
          port: 80
