---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: envoy-gateway
  labels:
    monitoring: otel
spec:
  endpoints:
    - port: metrics
      interval: "30s"
      path: /metrics
  selector:
    matchLabels:
      app.kubernetes.io/instance: envoy-gateway
