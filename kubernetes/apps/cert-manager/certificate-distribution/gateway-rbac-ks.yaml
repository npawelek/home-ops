---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: certificate-distribution-gateway-rbac
  namespace: cert-manager
spec:
  targetNamespace: gateway
  commonMetadata:
    labels:
      app.kubernetes.io/name: certificate-distribution
  dependsOn:
    - name: cert-manager
      namespace: cert-manager
  path: ./kubernetes/apps/cert-manager/certificate-distribution/gateway-rbac
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cluster-secrets
